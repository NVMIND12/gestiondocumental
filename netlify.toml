[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/decrees"
  to = "/.netlify/functions/decrees"
  status = 200

[functions]
  node_bundler = "esbuild"
